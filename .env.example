# OxideDB example environment file

# Listener address (defaults to 127.0.0.1:27017)
OXIDEDB_LISTEN_ADDR=127.0.0.1:27017

# PostgreSQL connection URL (optional for now)
# OXIDEDB_POSTGRES_URL=postgres://user:pass@localhost:5432/oxidedb

# OxideDB-specific log filter (overrides config.toml); supports tracing filter syntax
# Example: info,oxidedb=debug
OXIDEDB_LOG_LEVEL=info

# Standard Rust logging variable also supported (lower precedence than OXIDEDB_LOG_LEVEL)
# RUST_LOG=info,oxidedb=debug
